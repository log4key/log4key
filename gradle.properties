# ============================
# 各发布物独立版本
# CI 按 tag 以 -P 覆盖，例如：
#   tag v0.3.2          → -Plog4keyVersion=0.3.2
#   tag starter-v0.1.1  → -PstarterVersion=0.1.1
# ============================

# com.log4key:log4key-all（api + core 聚合）
log4keyVersion=0.3.1

# com.log4key:log4key-spring-boot-starter
starterVersion=0.1.0
